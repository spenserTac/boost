3rd Party Modules (modules/api's that you have to use pip install for) (This should normally be done with a development environment,
	and only the files that are used within the project will be shown)


1. django-bootstrap-modal-forms
(pip install django-bootstrap-modal-forms)
https://pypi.org/project/django-bootstrap-modal-forms/

2. django-filter
(pip install django-filter)
https://pypi.org/project/django-filter/

3. django-allauth
(pip install django-allauth)
https://pypi.org/project/django-allauth/

4. paypalhttp 
(pip install paypalhttp)
https://pypi.org/project/paypalhttp/

5. responses
pip install responses